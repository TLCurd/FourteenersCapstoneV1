@rec_area_reviews.map do |rec_area_review|
  {
    id: rec_area_review.id,
    user_id: rec_area_review.user_id,
    user_name: rec_area_review.user_name,
    user_email: rec_area_review.user_email,
    rec_area_id: rec_area_review.rec_area_id,
    review: rec_area_review.review
  }
end
